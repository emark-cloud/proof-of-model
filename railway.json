{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile --prod=false && pnpm build:ts && pnpm --filter @proof/dashboard build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @proof/dashboard start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
